{
  "rules": {
    ".read": false,
    ".write": false,
    "notifications": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": false,
        ".indexOn": ["created_at_ms"]
      }
    }
  }
}
